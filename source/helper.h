#ifndef HELPER_H
#define HELPER_H

enum CellType {SOLID = 1, BLUECORE = 2, IGNITED = 3, AIR = 4};
enum DirectionEnums {UP, DOWN, LEFT, RIGHT, FORWARD, BACKWARD, CENTER};

enum VelocityDirection { UDIR, VDIR, WDIR };

#endif